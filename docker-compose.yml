version: '3'
services:
  lupus-garupus-back:
    build: ./lupus-garupus-back
    environment:
      NODE_ENV: docker
      APP_LOG_LEVEL: debug
    ports:
      - "0.0.0.0:3030:3030"
    volumes:
      - ./lupus-garupus-back:/opt/lupus-garupus-back
